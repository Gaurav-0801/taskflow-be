# Backend2 Environment Variables
# This backend shares the same database as the original backend
# but runs on a different port (3002)

PORT=3002
DATABASE_URL=your-database-url-here
JWT_SECRET=your-jwt-secret-here
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development
